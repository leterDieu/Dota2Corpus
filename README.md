# Для контрибьюторов

Чтобы установить зависимости:  
```
pip install -r requirements.txt  
```

Если вы добавляете зависимость, добавьте ее в файл `requirements.txt`:  
```
pip freeze > requirements.txt  
```